defmodule Times do
  #def double(n), do: n * 2
  def double(n) do
    n * 2
  end

  #def triple(n), do: n * 3 
  def triple(n) do
    n * 3
  end

  #def quadruple(n), do: 2 * double(n)
  def quadruple(n) do
    2 * double(n)
  end
end
